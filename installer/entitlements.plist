<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for S1 Command Center (Tk/CustomTkinter
  + PyInstaller). These are the minimum required for notarization to
  succeed on a Python-frozen app:

  - allow-unsigned-executable-memory: Python's interpreter and Tk both
    generate executable memory at runtime (JIT-ish behavior, bytecode).
  - disable-library-validation: PyInstaller embeds dylibs that are not
    signed by the same Team ID as the outer bundle.
  - network.client: required for outbound HTTPS to S1 consoles.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
